[project]
name = "first_package"
version = "0.1.0"
description = "A simple Python package"
authors = [
    { name = "Niklas Chromik" }
]
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "numpy",
    "pandas",
    "click"
]